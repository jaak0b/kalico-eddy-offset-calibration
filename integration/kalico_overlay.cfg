# Appended to printer.cfg by the harness on a Kalico checkout. Stock Klipper
# always errors on an unused config option and rejects this section as
# unknown; Kalico only errors with it set, so appending it keeps the two
# firmwares checking the same thing.

[danger_options]
error_on_unused_config_options: True
