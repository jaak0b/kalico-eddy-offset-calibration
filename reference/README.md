# Reference: upstream tool_eddy_calibration.py

This file is copied unmodified from chengxg's `tool_eddy_calibration` project
(GPLv3), fetched via the thunderkeys/tool_eddy_calibration `translate-en`
fork. It is kept here as an algorithm reference only and is not loaded or
imported by this repository's plugin. Our `eddy_tool_calibration.py` ports
the algorithms it implements (directional scan fitting, forward/reverse pair
cancellation) rather than vendoring this code, and this file should not be
edited; upstream improvements are ported as ideas, not diffs.
